diff --git a/rog_map/include/rog_map_ros/rog_map_ros1.hpp b/rog_map/include/rog_map_ros/rog_map_ros1.hpp
index e181273..d20b64f 100644
--- a/rog_map/include/rog_map_ros/rog_map_ros1.hpp
+++ b/rog_map/include/rog_map_ros/rog_map_ros1.hpp
@@ -82,7 +82,7 @@ namespace rog_map {
             static tf2_ros::TransformBroadcaster br_map_ego;
             geometry_msgs::TransformStamped transformStamped;
             transformStamped.header.stamp = ros::Time::now();
-            transformStamped.header.frame_id = "world";
+            transformStamped.header.frame_id = cfg_.frame_id;
             transformStamped.child_frame_id = "drone";
             transformStamped.transform.translation.x = odom_msg->pose.pose.position.x;
             transformStamped.transform.translation.y = odom_msg->pose.pose.position.y;
@@ -282,7 +282,7 @@ namespace rog_map {
             }
             pcl::toROSMsg(pcl_cloud, cloud);
             cloud.header.stamp = ros::Time::now();
-            cloud.header.frame_id = "world";
+            cloud.header.frame_id = cfg_.frame_id;
         }
 
     public:
@@ -323,7 +323,7 @@ namespace rog_map {
         }
 
     private:
-        static void visualizeBoundingBox(visualization_msgs::MarkerArray& mkrarr,
+        void visualizeBoundingBox(visualization_msgs::MarkerArray& mkrarr,
                                          const Vec3f& box_min,
                                          const Vec3f& box_max,
                                          const string& ns,
@@ -341,7 +341,7 @@ namespace rog_map {
             int id = 0;
             visualization_msgs::Marker line_strip;
             line_strip.header.stamp = ros::Time::now();
-            line_strip.header.frame_id = "world";
+            line_strip.header.frame_id = cfg_.frame_id;
             line_strip.action = visualization_msgs::Marker::ADD;
             line_strip.ns = ns;
             line_strip.pose.orientation.w = 1.0;
@@ -396,7 +396,7 @@ namespace rog_map {
             mkrarr.markers.push_back(line_strip);
         }
 
-        static void visualizeText(visualization_msgs::MarkerArray& mkr_arr,
+        void visualizeText(visualization_msgs::MarkerArray& mkr_arr,
                                   const std::string& ns,
                                   const std::string& text,
                                   const Vec3f& position,
@@ -404,7 +404,7 @@ namespace rog_map {
                                   const double& size = 0.6,
                                   const int& id = -1) {
             visualization_msgs::Marker marker;
-            marker.header.frame_id = "world";
+            marker.header.frame_id = cfg_.frame_id;
             marker.header.stamp = ros::Time::now();
             marker.action = visualization_msgs::Marker::ADD;
             marker.pose.orientation.w = 1.0;
@@ -427,7 +427,7 @@ namespace rog_map {
             mkr_arr.markers.push_back(marker);
         };
 
-        static void visualizePoint(visualization_msgs::MarkerArray& mkr_arr,
+        void visualizePoint(visualization_msgs::MarkerArray& mkr_arr,
                                    const Vec3f& pt,
                                    Color color = Color::Pink(),
                                    std::string ns = "pt",
@@ -439,7 +439,7 @@ namespace rog_map {
             if (isnan(pt.x()) || isnan(pt.y()) || isnan(pt.z())) {
                 return;
             }
-            marker_ball.header.frame_id = "world";
+            marker_ball.header.frame_id = cfg_.frame_id;
             marker_ball.header.stamp = ros::Time::now();
             marker_ball.ns = ns.c_str();
             marker_ball.id = id >= 0 ? id : cnt++;
@@ -462,7 +462,7 @@ namespace rog_map {
             // add test
             if (print_ns) {
                 visualization_msgs::Marker marker;
-                marker.header.frame_id = "world";
+                marker.header.frame_id = cfg_.frame_id;
                 marker.header.stamp = ros::Time::now();
                 marker.action = visualization_msgs::Marker::ADD;
                 marker.pose.orientation.w = 1.0;
@@ -488,4 +488,4 @@ namespace rog_map {
     };
 }
 #endif // ROG_MAP_ROS_HPP
-#endif // USE_ROS1
\ No newline at end of file
+#endif // USE_ROS1
diff --git a/super_planner/CMakeLists.txt b/super_planner/CMakeLists.txt
index 226f253..6e2b083 100755
--- a/super_planner/CMakeLists.txt
+++ b/super_planner/CMakeLists.txt
@@ -81,21 +81,21 @@ target_link_libraries(fsm_node
         ${THIRD_PARTY}
 )
 
-#add_executable(read_replan_log
-#        Apps/read_replan_log.cpp
-#)
-#target_link_libraries(read_replan_log
-#        super
-#        -lncurses
-#        ${THIRD_PARTY}
-#)
-#
-#
-#add_executable(traj_opt_tuning
-#        Apps/traj_opt_tuning.cpp
-#)
-#target_link_libraries(traj_opt_tuning
-#        super
-#        -lncurses
-#        ${THIRD_PARTY}
-#)
+add_executable(read_replan_log
+        Apps/read_replan_log.cpp
+)
+target_link_libraries(read_replan_log
+        super
+        -lncurses
+        ${THIRD_PARTY}
+)
+
+
+add_executable(traj_opt_tuning
+        Apps/traj_opt_tuning.cpp
+)
+target_link_libraries(traj_opt_tuning
+        super
+        -lncurses
+        ${THIRD_PARTY}
+)
diff --git a/super_planner/config/click_smooth_ros1.yaml b/super_planner/config/click_smooth_ros1.yaml
index c7972e9..ec7375c 100755
--- a/super_planner/config/click_smooth_ros1.yaml
+++ b/super_planner/config/click_smooth_ros1.yaml
@@ -1,7 +1,8 @@
 fsm:
   click_goal_en: true
   click_goal_topic: "/goal"
-  click_height: 1.5
+  # A negative value keeps the z coordinate supplied by the autonomous decider.
+  click_height: -10.0
   click_yaw_en: true
   replan_rate: 15.0
   cmd_topic: "/planning/pos_cmd"
@@ -27,8 +28,8 @@ super_planner:
   planning_horizon: 7.0
   sensing_horizon: -1
   receding_dis: 4.0
-  robot_r: 0.2
-  yaw_dot_max: 3.0
+  robot_r: 0.35
+  yaw_dot_max: 1.0
   # Yaw mode: 1 heading to velocity, 2 heading to goal
   yaw_mode: 1
   mpc_horizon: 15
@@ -40,9 +41,11 @@ traj_opt:
     print_optimizer_log: false
 
   boundary:
-    max_vel: 5.0
-    max_acc: 5.0
-    max_jerk: 120.0
+    # Conservative limits for the first real task-one closed-loop tests.
+    # These stay below the command bridge limits (2.0 m/s, 3.0 m/s^2).
+    max_vel: 1.0
+    max_acc: 1.5
+    max_jerk: 20.0
     max_omg: 2.5
     max_acc_thr: 20.0
     min_acc_thr: 6.0
@@ -111,12 +114,15 @@ rog_map:
   unk_inflation_step: 1
   map_size: [ 50,50,6 ]
   # If the map rolling is disable, the map origin [m] should be set.
-  fix_map_origin: [ 0,0,1.5 ]
+  # Fast-LIO2 starts camera_init close to (0, 0, 0).
+  fix_map_origin: [ 0,0,0 ]
   frontier_extraction_en: false
 
   # Virtual ceil and ground
   virtual_ceil_height: 3.5
-  virtual_ground_height: -0.1
+  # Bench-integration value: leave margin below the current Fast-LIO2 origin.
+  # Recalibrate this from the lidar-to-floor height before flight.
+  virtual_ground_height: -2.0
 
   load_pcd_en: false
 
@@ -136,7 +142,7 @@ rog_map:
   ros_callback:
     enable: true
     cloud_topic: "/cloud_registered"
-    odom_topic: "/lidar_slam/odom"
+    odom_topic: "/Odometry"
     odom_timeout: 2.0
 
 
@@ -150,7 +156,7 @@ rog_map:
     frame_rate: 0
     # visualization range along the odom [m].
     range: [ 15, 15,4 ]
-    frame_id: "world"
+    frame_id: "camera_init"
     pub_unknown_map_en: false
 
   # Intensity filter for input point cloud. If the point intensity is less than this value,
@@ -185,4 +191,3 @@ rog_map:
     #             the cell will be considered as unknown.
     # in all, 1.0 is more aggressive while 0.0 is more conservative.
     unk_thresh: 1.0
-
diff --git a/super_planner/include/fsm/config.hpp b/super_planner/include/fsm/config.hpp
index 4f0aa43..a12331c 100755
--- a/super_planner/include/fsm/config.hpp
+++ b/super_planner/include/fsm/config.hpp
@@ -47,7 +47,7 @@ namespace fsm {
         double click_height{};
 
         bool click_yaw_en{};
-        string cmd_topic, mpc_cmd_topic, click_goal_topic;
+        string cmd_topic, mpc_cmd_topic, click_goal_topic, frame_id;
         double yaw_dot_max{};
 
         Config() = default;
@@ -63,6 +63,7 @@ namespace fsm {
             loader.LoadParam("fsm/cmd_topic", cmd_topic, string("/planning/pos_cmd"));
             loader.LoadParam("fsm/mpc_cmd_topic", mpc_cmd_topic, string("/planning_cmd/mpc"));
             loader.LoadParam("fsm/click_goal_topic", click_goal_topic, string("/planning/click_goal_topic"));
+            loader.LoadParam("rog_map/visualization/frame_id", frame_id, string("world"));
 
 
             loader.LoadParam("super_planner/yaw_dot_max", yaw_dot_max, 1.0, true);
diff --git a/super_planner/include/ros_interface/ros1/fsm_ros1.hpp b/super_planner/include/ros_interface/ros1/fsm_ros1.hpp
index 84135bf..6863559 100644
--- a/super_planner/include/ros_interface/ros1/fsm_ros1.hpp
+++ b/super_planner/include/ros_interface/ros1/fsm_ros1.hpp
@@ -35,12 +35,14 @@
 #include "nav_msgs/Odometry.h"
 #include "quadrotor_msgs/PositionCommand.h"
 #include "quadrotor_msgs/PolynomialTrajectory.h"
+#include "std_msgs/Bool.h"
 
 
 namespace fsm {
     class FsmRos1 : public Fsm {
         ros::NodeHandle nh_;
         ros::Subscriber goal_sub_;
+        ros::Subscriber mission_finished_sub_;
         ros::Publisher cmd_pub, mpc_cmd_pub_, path_pub_;
         ros::Timer execution_timer_, replan_timer_, cmd_timer_;
         quadrotor_msgs::PositionCommand pid_cmd_;
@@ -55,7 +57,7 @@ namespace fsm {
         }
 
         void publishCurPoseToPath() override {
-            path.header.frame_id = "world";
+            path.header.frame_id = cfg_.frame_id;
             path.header.stamp = ros::Time::now();
             geometry_msgs::PoseStamped pose;
             pose.header = path.header;
@@ -79,7 +81,7 @@ namespace fsm {
         void getOneHeartBeatMsg(quadrotor_msgs::PolynomialTrajectory &heartbeat, bool &traj_finish) {
             heartbeat.type = quadrotor_msgs::PolynomialTrajectory::HEART_BEAT;
             heartbeat.header.stamp = ros::Time::now();
-            heartbeat.header.frame_id = "world";
+            heartbeat.header.frame_id = cfg_.frame_id;
             double swt;
             planner_ptr_->getOneHeartbeatTime(swt, traj_finish);
             heartbeat.start_WT_pos = ros::Time(swt);
@@ -87,7 +89,7 @@ namespace fsm {
 
         void getCommittedTrajectory(quadrotor_msgs::PolynomialTrajectory &cmd_traj) {
             cmd_traj.header.stamp = ros::Time::now();
-            cmd_traj.header.frame_id = "world";
+            cmd_traj.header.frame_id = cfg_.frame_id;
             cmd_traj.type = quadrotor_msgs::PolynomialTrajectory::POSITION_TRAJ |
                             quadrotor_msgs::PolynomialTrajectory::HEART_BEAT;
             planner_ptr_->lockCommittedTraj();
@@ -137,7 +139,7 @@ namespace fsm {
             bool on_backup_traj;
             planner_ptr_->getOneCommandFromTraj(pvaj, yaw, yaw_dot, on_backup_traj, traj_finish);
             pos_cmd.header.stamp = ros::Time::now();
-            pos_cmd.header.frame_id = "world";
+            pos_cmd.header.frame_id = cfg_.frame_id;
             pos_cmd.position.x = pvaj(0, 0);
             pos_cmd.position.y = pvaj(1, 0);
             pos_cmd.position.z = pvaj(2, 0);
@@ -270,10 +272,30 @@ namespace fsm {
             setGoalPosiAndYaw(goal_p, goal_q);
         }
 
+        void missionFinishedCallback(const std_msgs::BoolConstPtr &msg) {
+            if (msg->data) {
+                if (!stop) {
+                    ROS_INFO("SUPER planning paused: exploration mission complete");
+                }
+                stop = true;
+                gi_.new_goal = false;
+                finish_plan = true;
+                machine_state_ = WAIT_GOAL;
+                return;
+            }
+            if (stop) {
+                ROS_INFO("SUPER planning resumed: exploration mission reset");
+                stop = false;
+                finish_plan = false;
+                machine_state_ = WAIT_GOAL;
+            }
+        }
+
         void init(const ros::NodeHandle &nh, const std::string &cfg_path) {
             // 初始化参数读取
             nh_ = nh;
             cfg_ = Config(cfg_path);
+            ros_interface::Ros1Adapter::setDefaultFrameId(cfg_.frame_id);
             map_ptr_ = std::make_shared<rog_map::ROGMapROS>(nh, cfg_path);
             // 初始化Planner
             ros_ptr_ = std::make_shared<ros_interface::Ros1Interface>(nh_);
@@ -290,6 +312,12 @@ namespace fsm {
                 cmd_cnt++;
             }
 
+            std::string mission_finished_topic;
+            nh_.param<std::string>("mission_finished_topic", mission_finished_topic,
+                                   "/mine_uav/exploration/finished");
+            mission_finished_sub_ = nh_.subscribe(
+                    mission_finished_topic, 1, &FsmRos1::missionFinishedCallback, this);
+
             if (cmd_cnt != 1) {
                 cout << YELLOW << " -- [Fsm] CMD INPUT ERROR." << RESET << endl;
                 exit(0);
@@ -360,4 +388,4 @@ namespace fsm {
 
 #endif //SRC_FSM_ROS1_HPP
 
-#endif
\ No newline at end of file
+#endif
diff --git a/super_planner/include/ros_interface/ros1/ros1_adapter.hpp b/super_planner/include/ros_interface/ros1/ros1_adapter.hpp
index ecfa016..40c4181 100644
--- a/super_planner/include/ros_interface/ros1/ros1_adapter.hpp
+++ b/super_planner/include/ros_interface/ros1/ros1_adapter.hpp
@@ -40,10 +40,21 @@ namespace ros_interface {
     using namespace geometry_utils;
     using namespace color_text;
 
-    const std::string DEFAULT_FRAME_ID = "world";
+    inline std::string &defaultFrameId() {
+        static std::string frame_id{"world"};
+        return frame_id;
+    }
 
     class Ros1Adapter {
     public:
+        static void setDefaultFrameId(const std::string &frame_id) {
+            defaultFrameId() = frame_id;
+        }
+
+        static const std::string &getDefaultFrameId() {
+            return defaultFrameId();
+        }
+
         /* For visualization ============================================================*/
         static void deleteAllMarkerArray(const ros::Publisher &pub_) {
             visualization_msgs::Marker del;
@@ -69,7 +80,7 @@ namespace ros_interface {
                 cloud.push_back(pt);
             }
             pcl::toROSMsg(cloud, pc2);
-            pc2.header.frame_id = DEFAULT_FRAME_ID;
+            pc2.header.frame_id = defaultFrameId();
             pc2.header.stamp = ros::Time::now();
         }
 
@@ -93,7 +104,7 @@ namespace ros_interface {
 
                 /* Publish point */
                 visualization_msgs::Marker point;
-                point.header.frame_id = DEFAULT_FRAME_ID;
+                point.header.frame_id = defaultFrameId();
                 point.header.stamp = ros::Time::now();
                 point.ns = ns.c_str();
                 point.id = point_id++;
@@ -119,7 +130,7 @@ namespace ros_interface {
                     geometry_msgs::Point p;
                     // publish lines
                     visualization_msgs::Marker line_list;
-                    line_list.header.frame_id = DEFAULT_FRAME_ID;
+                    line_list.header.frame_id = defaultFrameId();
                     line_list.header.stamp = ros::Time::now();
                     line_list.ns = ns + "_line";
                     line_list.id = line_cnt++;
@@ -171,7 +182,7 @@ namespace ros_interface {
             double vel_max = traj.getMaxVelRate();
             double map_factor = 1.0 / vel_max;
             last_pos = traj.getPos(0);
-            line_list.header.frame_id = DEFAULT_FRAME_ID;
+            line_list.header.frame_id = defaultFrameId();
             line_list.header.stamp = ros::Time::now();
             line_list.ns = name_space;
             line_list.action = visualization_msgs::Marker::ADD;
@@ -233,7 +244,7 @@ namespace ros_interface {
             if (show_waypoint) {
                 visualization_msgs::Marker marker_ball;
                 static int ball_id = 0;
-                marker_ball.header.frame_id = DEFAULT_FRAME_ID;
+                marker_ball.header.frame_id = defaultFrameId();
                 marker_ball.header.stamp = ros::Time::now();
                 marker_ball.ns = name_space + "_waypoints";
                 marker_ball.action = visualization_msgs::Marker::ADD;
@@ -345,7 +356,7 @@ namespace ros_interface {
                 static int line_cnt = 0;
                 /* Publish point */
                 visualization_msgs::Marker point;
-                point.header.frame_id = DEFAULT_FRAME_ID;
+                point.header.frame_id = defaultFrameId();
                 point.header.stamp = ros::Time::now();
                 point.ns = ns + " pt";
                 point.id = point_id++;
@@ -381,7 +392,7 @@ namespace ros_interface {
 
                 // publish lines
                 visualization_msgs::Marker line_list;
-                line_list.header.frame_id = DEFAULT_FRAME_ID;
+                line_list.header.frame_id = defaultFrameId();
                 line_list.header.stamp = ros::Time::now();
                 line_list.ns = ns + " line";
                 line_list.id = line_cnt++;
@@ -416,7 +427,7 @@ namespace ros_interface {
             geometry_msgs::Point point;
             meshMarker.id = mesh_id++;
             meshMarker.header.stamp = ros::Time::now();
-            meshMarker.header.frame_id = DEFAULT_FRAME_ID;
+            meshMarker.header.frame_id = defaultFrameId();
             meshMarker.pose.orientation.w = 1.00;
             meshMarker.action = visualization_msgs::Marker::ADD;
             meshMarker.type = visualization_msgs::Marker::TRIANGLE_LIST;
@@ -429,7 +440,7 @@ namespace ros_interface {
 
             edgeMarker = meshMarker;
             edgeMarker.header.stamp = ros::Time::now();
-            edgeMarker.header.frame_id = DEFAULT_FRAME_ID;
+            edgeMarker.header.frame_id = defaultFrameId();
             edgeMarker.id = edge_id++;
             edgeMarker.type = visualization_msgs::Marker::LINE_LIST;
             edgeMarker.ns = ns + " edge";
@@ -513,7 +524,7 @@ namespace ros_interface {
                     static int cnt = 0;
                     // publish lines
                     visualization_msgs::Marker line_list;
-                    line_list.header.frame_id = "world";
+                    line_list.header.frame_id = defaultFrameId();
                     line_list.header.stamp = ros::Time::now();
                     line_list.ns = ns;
                     line_list.id = cnt++;
@@ -571,7 +582,7 @@ namespace ros_interface {
             int id = 0;
             visualization_msgs::Marker line_strip;
             line_strip.header.stamp = ros::Time::now();
-            line_strip.header.frame_id = "world";
+            line_strip.header.frame_id = defaultFrameId();
             line_strip.action = visualization_msgs::Marker::ADD;
             line_strip.ns = ns;
             line_strip.pose.orientation.w = 1.0;
@@ -637,7 +648,7 @@ namespace ros_interface {
             if (isnan(pt.x()) || isnan(pt.y()) || isnan(pt.z())) {
                 return;
             }
-            marker_ball.header.frame_id = "world";
+            marker_ball.header.frame_id = defaultFrameId();
             marker_ball.header.stamp = ros::Time::now();
             marker_ball.ns = ns.c_str();
             marker_ball.id = id >= 0 ? id : cnt++;
@@ -672,7 +683,7 @@ namespace ros_interface {
             static int id = 0;
             mkr.id = id++;
             mkr.type = visualization_msgs::Marker::SPHERE;
-            mkr.header.frame_id = "world";
+            mkr.header.frame_id = defaultFrameId();
             mkr.header.stamp = ros::Time::now();
             mkr.ns = ns;
             mkr.id = id++;
@@ -701,7 +712,7 @@ namespace ros_interface {
             static int line_cnt = 0;
             /* Publish point */
             visualization_msgs::Marker point;
-            point.header.frame_id = "world";
+            point.header.frame_id = defaultFrameId();
             point.header.stamp = ros::Time::now();
             point.ns = ns.c_str();
             point.id = point_id++;
@@ -731,7 +742,7 @@ namespace ros_interface {
 
             // publish lines
             visualization_msgs::Marker line_list;
-            line_list.header.frame_id = "world";
+            line_list.header.frame_id = defaultFrameId();
             line_list.header.stamp = ros::Time::now();
             line_list.ns = ns + "line";
             line_list.id = line_cnt++;
diff --git a/super_planner/include/ros_interface/ros1/ros1_interface.hpp b/super_planner/include/ros_interface/ros1/ros1_interface.hpp
index fe73084..ff2e55e 100644
--- a/super_planner/include/ros_interface/ros1/ros1_interface.hpp
+++ b/super_planner/include/ros_interface/ros1/ros1_interface.hpp
@@ -353,7 +353,7 @@ namespace ros_interface {
             }
             sensor_msgs::PointCloud2 pc2;
             pcl::toROSMsg(pc, pc2);
-            pc2.header.frame_id = "world";
+            pc2.header.frame_id = Ros1Adapter::getDefaultFrameId();
             pc2.header.stamp = ros::Time::now();
             replan_log_pc_pub_.publish(pc2);
 
