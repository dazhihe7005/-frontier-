diff --git a/planner_control_interface/include/planner_control_interface/planner_control_interface.h b/planner_control_interface/include/planner_control_interface/planner_control_interface.h
index afe829c..6acb555 100644
--- a/planner_control_interface/include/planner_control_interface/planner_control_interface.h
+++ b/planner_control_interface/include/planner_control_interface/planner_control_interface.h
@@ -3,6 +3,7 @@
 
 #include <eigen3/Eigen/Dense>
 #include <geometry_msgs/PoseStamped.h>
+#include <geometry_msgs/PoseArray.h>
 #include <geometry_msgs/PoseWithCovarianceStamped.h>
 #include <interactive_markers/interactive_marker_server.h>
 #include <interactive_markers/menu_handler.h>
@@ -65,6 +66,7 @@ class PlannerControlInterface {
 
  private:
   ros::Publisher reference_pub_;
+  ros::Publisher diagnostic_raw_path_pub_;
   ros::Publisher planner_status_pub_;
   ros::Publisher stop_request_pub_;
   ros::Subscriber odometry_sub_;
diff --git a/planner_control_interface/src/planner_control_interface.cpp b/planner_control_interface/src/planner_control_interface.cpp
index 3780b51..aefc307 100644
--- a/planner_control_interface/src/planner_control_interface.cpp
+++ b/planner_control_interface/src/planner_control_interface.cpp
@@ -13,6 +13,8 @@ PlannerControlInterface::PlannerControlInterface(
     std::shared_ptr<PCIManager> pci_manager)
     : nh_(nh), nh_private_(nh_private) {
   reference_pub_ = nh_.advertise<visualization_msgs::Marker>("ref_pose", 5);
+  diagnostic_raw_path_pub_ = nh_.advertise<geometry_msgs::PoseArray>(
+      "/gbplanner/diagnostic_raw_path", 5);
   planner_status_pub_ = nh_.advertise<std_msgs::Bool>("gbplanner_status", 5);
   stop_request_pub_ = nh_.advertise<std_msgs::Bool>(
       "planner_control_interface/stop_request", 5);
@@ -743,6 +745,14 @@ void PlannerControlInterface::runPlanner(bool exe_path = false) {
             v_current_ = pci_manager_->getVelocity(path_type);
             // Publish the status
             publishPlannerStatus(plan_srv.response, true);
+            // Read-only diagnostic before PCI reconnects/interpolates the
+            // planner path. No consumer in the flight-control chain.
+            geometry_msgs::PoseArray diagnostic_path;
+            diagnostic_path.header.stamp = ros::Time::now();
+            diagnostic_path.header.seq = plan_srv.request.header.seq;
+            diagnostic_path.header.frame_id = world_frame_id_;
+            diagnostic_path.poses = plan_srv.response.path;
+            diagnostic_raw_path_pub_.publish(diagnostic_path);
             pci_manager_->executePath(plan_srv.response.path, path_to_be_exe,
                                       path_type);
             success = true;
