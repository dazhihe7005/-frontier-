# 任务一 40×40×30 m 仿真 A/B 验证（2026-09-16）

状态：**功能闭环曾完成，但安全验收未通过；不得据此真机自主飞行。** 本报告只讨论任务一，同一场景入口中高平台、30 m Gazebo MID360 近似扫描、PX4 SITL、SUPER 和 NUC 决策链。仿真点云不等于真实 MID360+Fast-LIO2 验证。

## 因果链一：空旷前方无目标

原始扫描中大量超过传感器 30 m 的无回波射线以约 70 m 有限坐标表示；适配器以前全部丢弃。决策器只对障碍回波做 raycast，前方空旷但不标记自由，因而拒绝未知区目标。诊断：同一入口附近 `free_forward≈3.2 m`，首个目标完成后约 80 s 无后继。修复仅作用于 Gazebo 适配层：把超范围射线截到 29.5 m，单独作为自由射线端点；决策器只对该可选话题标记自由，绝不作为占据点传给 SUPER；实机默认不订阅该话题。修后入口附近 `free_forward≈28 m`，连续向前目标恢复。前一项目标交接也改成“先确认可达后继，再取消旧目标”。

## A/B 实测

| 运行 | 结果 | 核心证据 |
| --- | --- | --- |
| run02：只修目标交接 | 失败 | 首目标后长时间无新目标，最深约 8.1 m，未返航。 |
| run03：加入仿真自由射线 | 功能闭环，但安全不通过 | 连续 6 个前进目标，最深约 44.25 m；前墙深度约 47.5 m，左右墙覆盖约 0.83/0.85；地图闭合并返航，home 误差 0.165 m，切 AUTO.LOITER。返航最大横向偏移 9.268 m，规划轨迹本身向侧方绕行并下沉。 |
| run04：沿实际去程位置反向分段返航 | 失败 | 返航横向偏移降到 2.301 m，但在约 x=8–12 m 入口附近，SUPER 轨迹上抬到约 2.7 m，超过桥接器 1.8 m 安全高度；`HEIGHT_GEOFENCE` 触发，未完成返航。约 626 条 SUPER 失败日志。 |

run03 的直接 home 目标从 x≈44 m 一跳到 x≈0 m；bag 中 `/planning/pos_cmd` 已包含侧移和下降，因此大偏移不是 PX4 单独跟踪误差。run04 的返航子目标来自真实去程位置，每约 4 m 一个；这解决了长目标造成的广域绕行，但暴露近入口局部地图/安全走廊/优化器问题。不能因 run03 曾到 home 就认定安全闭环。

run03 记录：`/home/nuc/task1_logs/failure_cases/goal_lifecycle_fixed/task1_40m_free_rays_run03.bag` 及同名 JSON；run04：`task1_40m_breadcrumb_run04.bag` 及同名 JSON。bag 保存在本机，不提交 Git 大文件。run03 目标生命周期统计：7 次取消，取消后旧指令 0、旧 `PlanFromRest` 0、桥接故障 0。run04 桥接故障来自规划高度越界，而不是取消协议。

## 尚待证明及验收

1. 录取入口附近 ROGMap 占据/膨胀地图、输入点云、SUPER 安全走廊和规划轨迹，明确 x≈8 m 处规划失败的物理/数据原因；核对是否为平台、误回波、未知空间还是地图索引缺陷。不能只提高桥接高度上限。
2. 规划器纵向可行域必须与桥接器 `z≤1.8 m` 一致。目前 `rog_map.virtual_ceil_height: 4.0` 经 ROGMap 量化/膨胀后也不等于 1.8 m 的指令约束；SUPER 仍能生成超限轨迹。修复应在规划约束层保证，而非仅在桥接器拒绝。
3. 在同一 40 m 场景重跑到完整返航，要求无桥接故障、无旧目标指令、横向偏差在可接受走廊内、真实高度不越界、完成标志和 AUTO.LOITER。再跑不同尺寸/入口几何场景验证适配性。
4. 当前通用验收脚本默认中位速度≥1.5 m/s、OFFBOARD≤90 s，而本次 SUPER 配置 `max_vel=1.0 m/s`，两项不能同时满足。必须建立按任务配置的速度/时长标准，不能简单把失败标记忽略；近尽头为地图闭合等待的低速应与非预期停滞分开评估。
