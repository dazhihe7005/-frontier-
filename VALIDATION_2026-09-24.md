# GBPlanner2 全链路验收记录

日期：2026-09-24（Asia/Shanghai）

## 自动验收结果

最终标准世界冷启动中，`validate_full_chain.py _duration:=60` 提前满足所有
通过条件并返回：

```json
{
  "passed": true,
  "checks": {
    "mid360_to_fastlio": true,
    "fastlio_running": true,
    "fastlio_to_gbplanner": true,
    "fastlio_to_px4": true,
    "voxblox_integrated": true,
    "gbplanner_planned": true,
    "executor_streaming": true,
    "trajectory_to_px4": true,
    "px4_dynamic_flight": true
  },
  "counts": {
    "raw_mid360": 65,
    "fastlio_input": 65,
    "fastlio_odometry": 65,
    "planner_odometry": 65,
    "px4_vision_input": 195,
    "voxblox_input": 65,
    "voxblox_map": 8,
    "gbplanner_trajectory": 1,
    "px4_setpoint": 325
  },
  "max_trajectory_points": 130,
  "max_px4_displacement_m": 3.9585,
  "setpoints_finite": true,
  "px4_states_seen": ["CONNECTED", "ARMED", "OFFBOARD"]
}
```

## 最终延长运行观察

- 最终冷启动无需手工干预，从起飞、悬停到自动探索均正常。规划器启动于
  仿真时间 19.5 s，先执行三个 0.38--0.99 m 的有效短航段，27.9 s 生成
  第一条 5.02 m 长轨迹；不再等待到约 93 s 才开始连续探索。
- 随后轨迹端点连续推进到 x≈62.6 m，主要航段为 4.1--5.0 m。x≈46.4 m
  处曾连续出现 8 次空决策，但约 5 s 后自行恢复并继续到 49.7、53.7、
  58.6、62.6 m；未回原点、未触发 PX4 failsafe。
- 方向保持连续且没有硬性 `+X` 候选过滤。短航段和反向候选均允许，
  因而不会牺牲未知分支探索或弯道微调。
- Voxblox 同步日志持续显示点云与里程计 `dt=+0.0000 s`。
- 控制链路没有订阅 Gazebo 位姿真值。

## 路口专项结果

- 使用 1:1 白象山模型的旋转验证世界，使真实交叉口位于标准起点前方。
- 修正方向代价后，轨迹从 x≈8.1 m 到 x≈13.2 m（路口中心），随后继续到
  x≈17.6 m；路口处没有原地绕圈、掉头或连续空规划。
- 该次专项试验随后用于测试过一个缓升起飞方案，后者引起高度振荡，已经
  撤回，不进入最终配置；路口通过证据发生在振荡之前。

## 已修复的动态问题

1. 原点/路口空规划：原始激光帧长期使用前一帧约 0.10 s 的位姿，加上
   TSDF 权重 200、清除权重 0.01，形成进入 1 m 安全包络的历史障碍层。
   现用时间同步器配对扫描和 FAST-LIO2 位姿，并提高可清除性。
2. 持续向顶板爬升：MID360S 原被建模为对称 ±45°，再叠加 25°安装俯角，
   信息增益错误偏向顶板。现采用 MID360S 原生 -7°/+52°垂直视场，并在
   GBPlanner 的对称传感器模型中使用等效光轴 2.5°。
3. 立即掉头：方向软惩罚仅 0.05，且上游方向参考轨迹循环把所有点写成
   同一终点。公式已修正，软权重改为 0.30，仍不构成方向硬约束。
4. 起飞断流：PX4 允许最高 3.0 m/s 起飞，而外部视觉桥旧阈值为 2.0 m/s；
   2.04 m/s 的合法瞬态会先断视觉，再导致 failsafe。门槛已改为 3.5 m/s，
   同时保留 2 m 单帧跳变保护，规划速度仍为 0.7 m/s。
5. 重复启动污染：PX4 会在运行目录保存自动校准偏置。启动脚本现默认为
   每次仿真创建独立目录，异常运行不会污染下一次冷启动。
6. 起点等待/互锁：执行器曾拒绝首条小于 1 m 的轨迹，但 PCI 只在当前轨迹
   完成后触发下一次规划，构成“执行器等待长轨迹、PCI 等待轨迹完成”的
   相互等待。现已删除首段最短长度硬门槛；结构非法、跳变、超速和越界
   校验仍保留。1 m 只表示碰撞安全包络，不是最小航段长度。
7. 随机起飞高度发散：ULog 对比显示失控前 Gazebo 真值仍在地面，而 PX4
   高度估计已漂移约 7 m，静止 IMU 横向冲击约达 ±4 m/s²。STEP 最低结构
   位于原点下方 0.2718 m，旧碰撞体却只到 -0.05 m，导致起落架穿地、机腹
   压在粗糙网格上。现增加两条 STEP 尺寸的起落滑橇碰撞体，在原始 1:1
   地图内增加仅覆盖起落架足迹的平整起飞台；台面 z=0.20 m，机体出生
   z=0.4718 m。
8. 起点净空不足：原 y=0.5 m 位置在飞行高度距离实测巷壁约 0.95 m，物理上
   已不满足用户指定的 1 m 安全半径。保持地图比例和安全半径不变，将出生
   点横移到 y=0.2 m 后，最近水平墙面约 1.31 m，规划空间重新可行。
9. 悬停后迟迟不探索：旧流程在起飞交接时清除约 17 s 已成熟的 Voxblox
   地图，规划器只能从稀疏重建地图中反复给出短航段。接触和时序问题修复后
   不再清图，只保留 2 s 稳定窗口；第一条约 5 m 轨迹由约 93 s 提前至
   27.9 s。

因此建图是原因之一，但不是唯一原因。完整因果链是：扫描/位姿错时和高
TSDF 持久性造成伪障碍，错误传感器视场和方向参考扭曲评分；起点本身不满足
1 m 净空且 STEP 接触几何不完整，污染状态估计；随后清除成熟地图和首段
1 m 硬门槛又分别造成稀疏决策与执行互锁。上述环节均已分别修复。

## 长时间图形化运行追加发现

首次图形化延长运行在 x≈59.5 m 遇到一次低局部增益后，把“当前全局图未
找到 frontier”直接解释为完全探索，并生成 59.67 m、89.55 s 的整程返航
轨迹。ULog 证明轨迹设定点本身约 0.67 m/s 且连续，但外部视觉桥将每个
10 Hz FAST-LIO2 估计以 30 Hz 重复发布，并把约 0.2 s 前的测量重标成当前
时刻。返航后段的估计相位差逐渐放大，仿真时间 202.064 s 首次超过
3.5 m/s 输入保护，视觉安全停流；PX4 于 203.104 s 检测到 Offboard 信号
丢失，最终失效保护落地并解除武装。

直接把 Gazebo `/clock` 的旧测量时间交给尚未按该延迟配置的 PX4 会使位置
状态过期，因此最终方案是每个 FAST-LIO2 输入只发布一次、使用 MAVROS 当前
传输时间，并在隔离的 1018 空机架中用 `EKF2_EV_DELAY` 显式补偿测得延迟。
`go_home_if_fully_explored` 改为 `false`：单次低增益
和一次 frontier 未命中只会继续探索重试，不再自动执行几十米返航；显式及
预算触发的返航仍由 `auto_homing_enable` 和 homing 服务保留。

## 空规划恢复与路口长时回归

修正后的标准世界首先连续运行到 x≈86.7 m。FAST-LIO 点云和里程计持续更新、
PX4 保持 armed/OFFBOARD，但 GBPlanner 连续 330 次在同一位置返回空路径，证明
该停滞不是定位或建图进程中断，而是“局部树无增益、全局图无保留 frontier、
根节点不变化”形成的决策闭环。

现增加独立 stall recovery 状态机：15 次连续空结果后暂停 PCI，反向执行最近
一段已实际执行且通过碰撞检查的路径，保持当前航向、限速 0.45 m/s，随后恢复
自动规划。它只使用 GBPlanner 历史轨迹和 FAST-LIO 里程计，不读取 Gazebo
真值，不改变固定 1 m 碰撞安全包络。

在 1:1 白象山旋转路口场景中，系统于 x≈50.6 m 检测到持续空规划，第一次
沿安全历史轨迹回退 5.43 m（12.06 s）并自动恢复；第二次短暂回退后越过原
停滞点，轨迹从 `(59.3, 2.7)` 经 `(62.8, -1.5)`、`(66.1, -4.5)` 转入真实
分支，并继续推进到 FAST-LIO 坐标约 `(91.3, -20.3, 1.46) m`。仿真时间
293 s 停止测试前，PX4 仍为 connected/armed/OFFBOARD，vision 状态为
`STREAMING`，未发生 failsafe 或降落。

同一次回归中的 60 s 自动验收再次 9/9 通过：记录到 75 帧 MID360、76 帧
FAST-LIO 里程计和 76 帧 PX4 视觉输入，证明视觉输入与 FAST-LIO 新帧一一
对应；同时记录 374 个 PX4 设定点、最大动态位移 4.5068 m，所有设定点有限。

## 构建与静态检查

- `catkin build planner_common gbplanner mine_uav_gbplanner`：22 个构建包全部成功。
- Python 脚本通过 `py_compile`。
- launch、SDF、world XML 均通过 `xmllint`，全部 YAML 可解析。
- 两个第三方修改均保存为可反向校验的补丁文件。
- 最终 60 s 在线验证 9/9 项通过：MID360S→FAST-LIO2、FAST-LIO2→
  Voxblox/GBPlanner/PX4、GBPlanner→执行器→PX4、动态飞行均有实时证据。
