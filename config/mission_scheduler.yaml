# ROS RC indexes are zero-based. Confirm physical CH7 and CH11 in MAVROS/QGC.
rc_topic: "/mavros/rc/in"
goaf_trigger_channel: 6
shaft_trigger_channel: 10

# Both stable low->high and high->low changes are start events. The first
# stable reading after startup only establishes a baseline.
low_threshold: 1300
high_threshold: 1700
switch_stable_time: 0.5
rc_timeout: 1.0

# Task 2 remains blocked in production until its real sensor and PX4 control
# chain is validated. Its dedicated SITL launch may override this flag.
shaft_task_available: false

# Task-one Fast-LIO2 gate. Task two uses its own depth and range validation.
odometry_topic: "/Odometry"
odometry_timeout: 1.0
require_odometry: true

mavros_state_topic: "/mavros/state"
require_mavros_connection: true

decision_rate: 10.0

active_task_topic: "/mine_uav/mission/active_task"
auto_enable_topic: "/mine_uav/mission/auto_enable"
goaf_enable_topic: "/mine_uav/mission/goaf_enable"
shaft_enable_topic: "/mine_uav/mission/shaft_enable"
return_home_topic: "/mine_uav/mission/return_home"
switch_event_topic: "/mine_uav/mission/switch_event"
status_topic: "/mine_uav/mission/status"
