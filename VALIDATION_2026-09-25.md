# GBPlanner2 长时安全验收记录

日期：2026-09-25（Asia/Shanghai）

## 验收对象

- 白象山 1:1 实景巷道；
- 6 kg 定制无人机、PX4 1018 无 GPS 空机架；
- Gazebo MID360S 点云 → FAST-LIO2 → Voxblox/GBPlanner2；
- FAST-LIO2 水平位置 → PX4 external vision XY；
- GBPlanner2 轨迹 → 安全执行器 → PX4 OFFBOARD；
- 固定 1.0 m 参考点安全半径。

Gazebo 真值只由独立验证器订阅，用于离线误差统计，没有进入定位、建图、
规划、轨迹执行或 PX4 控制链路。

## 300 秒全链路结果

命令：

```bash
rosrun mine_uav_gbplanner validate_full_chain.py \
  _duration:=300 _observe_full_duration:=true _min_displacement:=20
```

冷启动后完整观察 300.0 s，验证结果 `passed=true`，全部 12 项检查通过：

- MID360S 原始点云 3000 帧，FAST-LIO2 输入 3000 帧；
- FAST-LIO2 里程计、GBPlanner2 里程计和 PX4 视觉输入各 2950 帧；
- Voxblox 输入 2950 帧、地图输出 300 帧；
- GBPlanner2 输出 26 条轨迹，最长 924 个采样点；
- PX4 收到 14027 个有限设定点，全程出现 CONNECTED、ARMED、OFFBOARD；
- 最大 PX4 位移 62.9875 m；
- 最大命令速度 0.5051 m/s，最大实际速度 0.6841 m/s；
- 最大轨迹跟踪误差 0.5206 m，跟踪自适应降速正常工作；
- 最小水平净空 1.2949 m；
- 最小三维净空 1.2357 m；
- 水平和三维净空小于 1.0 m 的样本均为 0；
- 飞行期间外部视觉不健康样本为 0；
- 触发 5 次实时安全阻塞、2 次已执行轨迹回退恢复，均未造成安全半径侵入。

相对于仅用于诊断的 Gazebo 真值，PX4/FAST-LIO2 相对轨迹最大误差为
1.1169 m，FAST-LIO2 原始相对轨迹最大误差为 2.8327 m。以本次约 63 m 的
探索尺度计，闭环仍保持连续且没有使用真值修正。

## 本轮修复的碰撞因果链

1. 原自反射过滤柱向下延伸到机体 z=-0.65 m，把无人机正下方逐渐接近的
   地面回波一并删除，规划和执行保护层都无法及时看到地面。
2. 旧保护只按接近水平面的薄切片计算净空，带小幅升降的长轨迹可能进入
   顶板/地面 1 m 包络而不提前制动。
3. 安全停车时把发送给 PX4 的零速度误写回“计划运动方向”，下一帧方向
   走廊变为无定义并释放停车，形成停—走抖动。
4. FAST-LIO2 的长期 Z 漂移曾直接改变物理飞行高度，使正确的水平路径在
   巷道起伏处接近顶板。现在 PX4 继续使用气压高度，每条新轨迹只把相对 Z
   变化重新锚定到当前 PX4 高度。

现配置把机体自过滤限制在实际结构范围，使用水平与完整三维净空双重保护，
保留停车前的计划方向，并在接近顶板/地面时锁存一次 0.35 m 反向垂直脱离。
因此本次长时运行通过了此前发生碰撞的区段，并保持至少 1.2357 m 实测净空。

## 适用边界

该结果证明当前确定性白象山仿真、当前传感器噪声和 0.5 m/s 规划速度下的
全链路验收通过，不等于能数学证明任意地图、任意噪声或真机条件下“绝对零
故障”。2 m/s 已在同一 6 kg 模型上证明不安全，当前配置不再启用该速度。
