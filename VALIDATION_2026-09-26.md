# GBPlanner2 提速验证记录

日期：2026-09-26（Asia/Shanghai）

## 目标与配置

在隔离工作区的白象山 1:1 实景巷道中，将开阔段规划上限从 0.5 提至
0.7 m/s；狭窄环境上限维持 0.35 m/s，1 m 三维安全半径不变。PX4 1018
无 GPS 空机架、MID360S → FAST-LIO2 → Voxblox/GBPlanner2 → PX4 OFFBOARD
全链路保持不变，Gazebo 真值仅供离线诊断。

执行器允许 0.84 m/s 的轨迹校验上限，避免正常 0.7 m/s 轨迹因插值小幅
超限被拒。前进方向的普通制动余量改为 1.60/0.75 m（开始降速/停止），
frontier 末端改为 1.85/0.95 m；全方向三维净空带仍为 1.55/1.25 m，
垂直避让触发净空仍为 1.30 m。

## 主场景 300 秒冷启动验收

```bash
./run_full_chain.bash gui:=false rviz:=false
rosrun mine_uav_gbplanner validate_full_chain.py \
  _duration:=300 _observe_full_duration:=true _min_displacement:=20
```

验收器 `passed=true`，12 项检查全部通过：

- 观测 300.0 s；MID360 原始点云 2998 帧，FAST-LIO2 里程计 2945 帧，
  PX4 外部视觉输入 2944 帧；
- GBPlanner2 输出 56 条轨迹，PX4 收到 13964 个有限设定点；
- 最大 PX4 位移 91.8376 m，上一版 0.5 m/s 冷启动记录为 62.9875 m。
  两次规划路径不完全相同，此对比是单次测试结果，不是严格的固定路线
  速度倍率；
- 最大轨迹命令速度 0.7037 m/s，最大实际速度 0.8697 m/s；在实际
  速度至少 0.2 m/s 的 6869 个采样中，平均实际速度 0.5404 m/s；
- 最近三维净空 1.2106 m，低于 1 m 的采样 0；最近水平薄层净空
  1.3074 m；
- 9 次实时安全阻塞、4 次回退恢复，期间外部视觉不健康采样 0，PX4
  持续处于已连接、已解锁和 OFFBOARD 链路中。

## 参数回退与恢复节点修正

最初同时扩大了全方向顶板/地面净空带：三维减速/停止阈值升到
1.65/1.30 m，垂直避让阈值升到 1.35 m。巷道的正常顶板净空约
1.34 m，因而反复触发垂直避让；一次 155.7 s 中途诊断的最大位移仅
7.7055 m，运动均速 0.4365 m/s。此组合已撤销，只保留沿实际运动
方向提前制动。

回退节点原先取最近一条被保护层在起点挡住的轨迹，反向可行距离为零，
于是反复原地重规划。现在向前查找历史中首条可安全反向至少 0.5 m 的
已接受轨迹；回退仍按实时 MID360 的 1 m 硬约束执行，不用 Gazebo 真值。

## 交叉口专项世界

先发现该隔离测试世界的 180° 旋转平移误写成 `(-14, 1)`：标准起点
`(-7, 0.2)` 对应的正确平移应是 `(-14, 0.4)`。误写使巷道相对起点
侧移 0.6 m，起飞点最小三维净空仅 0.7799 m；此初次测试在
50.6 s 时终止，不是速度引起的飞行侵入。修正的是测试世界注册位置，
原始白象山网格、比例和生产场景均未改动。

修正后重新冷启动并观察完整 180.0 s，验收器 `passed=true`：最大
PX4 位移 14.1379 m，最大命令/实际速度 0.7000/0.8644 m/s，运动
均速 0.4539 m/s；最小三维净空 1.1555 m，低于 1 m 的样本 0；
外部视觉不健康采样 0。该次探索中发生 6 次安全阻塞、2 次回退恢复。
规划器最终选择回退到起点另一侧，故此记录不能证明按指定方向通过
岔口；只能证明该变体中的闭环运行和观测到的轨迹仍满足 1 m 约束。

本验证仅说明当前地图、车辆和噪声配置的 0.7 m/s 开阔段目标通过一次
300 s 回归；不保证所有未知巷道均能持续达到该速度，更不批准 2 m/s
真机运行。
